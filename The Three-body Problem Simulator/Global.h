#pragma once

#include <iostream>
#include <stdio.h>
#include <math.h>
#include <Windows.h>
#include <vector>

using namespace std;

template<typename T>
constexpr auto SQR(T a) { return ((a)*(a)); }

constexpr int ONEHUNDRED = 100;
