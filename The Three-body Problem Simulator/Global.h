#pragma once

#include <iostream>
#include <math.h>
#include <time.h>
#include <Windows.h>
#include <vector>

using namespace std;

template<typename T>
constexpr auto SQR(T a) { return ((a)*(a)); }

constexpr auto DT = 1;
